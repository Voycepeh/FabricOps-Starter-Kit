# range

Checks one column using a minimum, maximum, or both. Each bound has an independent inclusive flag.

```json
{"rule_type":"range","columns":["amount"],"minimum":0,"minimum_inclusive":true,"maximum":100,"maximum_inclusive":false}
```
